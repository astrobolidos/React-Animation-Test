{
  "name": "React-Animation-Test",
  "version": "0.0.1",
  "description": "A animation tests with nested collections",
  "main": "js/app.js",
  "scripts": {
    "start": "watchify -o js/bundle.js -v -d .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "lisandro pacheco",
  "license": "ISC",
  "dependencies": {
    "react": "^0.13.1"
  },
  "devDependencies": {
    "browserify": "^9.0.3",
    "reactify": "^1.1.0",
    "watchify": "^2.6.1"
  },
  "browserify": {
    "transform": [
      "reactify"
    ]
  }
}
